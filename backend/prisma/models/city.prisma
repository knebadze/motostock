model City {
  id           Int           @id @default(autoincrement())
  key          String        @unique
  nameKa       String
  nameEn       String
  nameRu       String
  isTbilisi    Boolean       @default(false)
  addresses    Address[]
  companyInfos CompanyInfo[]

  @@schema("cla")
}
