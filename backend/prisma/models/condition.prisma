model Condition {
  id              Int              @id @default(autoincrement())
  key             String           @unique
  nameKa          String
  nameEn          String
  nameRu          String
  listings        VehicleListing[]
  productVariants ProductVariant[]

  @@schema("cla")
}
