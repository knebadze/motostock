model Size {
  id       Int              @id @default(autoincrement())
  key      String           @unique
  nameKa   String
  nameEn   String
  nameRu   String
  variants ProductVariant[]

  @@schema("cla")
}
