// Single source of truth for the brand name — used in emails, API docs, and
// seed data instead of hardcoding "MotoStock" (or a wrong variant of it) in
// multiple places.
export const SITE_NAME = "MotoStock 22";
