E2E_BASE_URL=http://localhost:3000
E2E_BACKEND_URL=http://localhost:4000
# Only used by test-teardown helpers that clean up test-created DB rows
# directly (e.g. a customer account from the auth spec) — same value as
# backend/.env's DATABASE_URL.
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/motostock"
